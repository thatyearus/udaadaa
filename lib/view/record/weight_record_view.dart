import 'package:flutter/material.dart';

class WeightRecordView extends StatelessWidget {
  const WeightRecordView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Weight Record View'),
      ),
    );
  }
}
