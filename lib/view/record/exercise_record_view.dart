import 'package:flutter/material.dart';

class ExerciseRecordView extends StatelessWidget {
  const ExerciseRecordView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Exercise Record View'),
      ),
    );
  }
}
