import 'package:flutter/material.dart';

class FoodRecordView extends StatelessWidget {
  const FoodRecordView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Food Record View'),
      ),
    );
  }
}
