import 'package:flutter/material.dart';

class MyRecordView extends StatelessWidget {
  const MyRecordView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('My Record View'),
      ),
    );
  }
}
